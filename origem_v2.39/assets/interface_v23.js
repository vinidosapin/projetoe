
(()=>{const root=document.querySelector('[data-discipline-browser]');if(!root)return;const input=root.querySelector('#discipline-search');const buttons=[...root.querySelectorAll('[data-filter]')];const cards=[...root.querySelectorAll('.subject-card')];const empty=root.querySelector('.no-results');let area='all';const norm=s=>(s||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase();function apply(){const q=norm(input.value.trim());let shown=0;cards.forEach(c=>{const okArea=area==='all'||c.dataset.area===area;const okText=!q||norm(c.textContent).includes(q);const show=okArea&&okText;c.hidden=!show;if(show)shown++});empty.classList.toggle('show',shown===0)}buttons.forEach(b=>b.addEventListener('click',()=>{area=b.dataset.filter;buttons.forEach(x=>x.setAttribute('aria-pressed',String(x===b)));apply()}));input.addEventListener('input',apply);})();
